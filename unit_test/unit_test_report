controller class:
    function contained:
             [PASS]    def toggle_debug_mode(self)
             [PASS]    def start_game(self)
             [PASS]    def game_loop(self)  // unnecessary check
             [PASS]    def player_turn(self, player_id)
             [PASS]    def handle_dice_throw(self, player_id)
             [PASS]    def handle_square(self, player_id, position)
             [PASS]    def check_bankruptcy()
             [PASS]    def handle_jail(self, player_id)
             [PASS]    def end_game(self)
             [PASS]    def handle_debug_action
total tests:     29
tests passed:    29

coverage: 100%

map_editor class:
    function contained:
             [PASS]    def add_square(self, position, square)
             [PASS]    def edit_square(self, position, square)
             [PASS]    def get_square(self, position)
             [PASS]    def validate_map(self)
             [PASS]    def to_dict(self)
             [PASS]    def from_dict(data)
             [PASS]    def display_map_summary(map_data)
total tests:     15
tests passed:    15

coverage: 100%

model class:
    function contained:
             [PASS]    def load_game(self, save_file)
             [PASS]    def save_game(self)
             [PASS]    def setup_new_game(self, map_file, player_names)
             [PASS]    def update_player_position(self, player_id, new_position)
             [PASS]    def update_player_cash(self, player_id, amount)
             [PASS]    def update_player_in_jail(self, player_id, in_jail)
             [PASS]    def update_player_jail_turns(self, player_id, turns)
             [PASS]    def update_round_num(self, new_round_num)
             [PASS]    def is_game_over(self)
             [PASS]    def get_winners(self)

             [PASS]    prompt
             [PASS]    display
             [PASS]    display_options
             [PASS]    display_map_summary
             [PASS]    sidplay_errors

             [PASS]    create_new_map
             [PASS]    define_square
             [PASS]    edit_map
             [PASS]    load_map
             [PASS]    main_menu
total tests:     14
tests passed:    14
coverage: 100%


view class:
    function contained:
             [PASS]    test_welcome
             [PASS]    test_choose_if_use_save_yes
             [PASS]    test_choose_if_use_save_no
             [PASS]    test_choose_if_use_save_invalid_then_yes
             [PASS]    test_choose_save_file_valid
             [PASS]    test_choose_save_file_no_files
             [PASS]    test_choose_save_file_no_directory
             [PASS]    test_choose_new_game_map_valid
             [PASS]    test_choose_new_game_map_no_files
             [PASS]    test_choose_new_game_map_no_directory
             [PASS]    test_choose_player_num_valid
             [PASS]    test_choose_player_num_invalid_then_valid
             [PASS]    test_choose_player_name
             [PASS]    test_choose_player_name_invalid_space
total tests:     14
tests passed:    14
coverage: 100%
Total_unit_test_coverage:100%