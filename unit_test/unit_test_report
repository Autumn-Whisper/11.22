controller class:
    function contained:
             [check]    def toggle_debug_mode(self)
             [check]    def start_game(self)
                        def game_loop(self)  // unnecessary check
             [check]    def player_turn(self, player_id)
             [check]    def handle_dice_throw(self, player_id)
             [check]    def handle_square(self, player_id, position)
             [check]    def check_bankruptcy()
             [check]    def handle_jail(self, player_id)
             [check]    def end_game(self)
                        def handle_debug_action
total tests:     22
tests passed:    22

coverage: 88.89%

map_editor class:
    function contained:
             [check]    def add_square(self, position, square)
             [check]    def edit_square(self, position, square)
             [check]    def get_square(self, position)
             [check]    def validate_map(self)
             [check]    def to_dict(self)
             [check]    def from_dict(data)
             [check]    def display_map_summary(map_data)
total tests:     15
tests passed:    15

coverage: 100%

model class:
    function contained:
             [check]    def load_game(self, save_file)
             [check]    def save_game(self)
             [check]    def setup_new_game(self, map_file, player_names)
             [check]    def update_player_position(self, player_id, new_position)
             [check]    def update_player_cash(self, player_id, amount)
             [check]    def update_player_in_jail(self, player_id, in_jail)
             [check]    def update_player_jail_turns(self, player_id, turns)
             [check]    def update_round_num(self, new_round_num)
             [check]    def is_game_over(self)
             [check]    def get_winners(self)

             [check]    prompt
             [check]    display
             [check]    display_options
             [check]    display_map_summary
             [check]    sidplay_errors

             [check]    create_new_map
             [check]    define_square
             [check]    edit_map
             [check]    load_map
             [check]    main_menu
total tests:     14
tests passed:    14
coverage: 100%


view class:
    function contained:
             [PASS]    test_welcome
             [PASS]    test_choose_if_use_save_yes
             [PASS]    test_choose_if_use_save_no
             [PASS]    test_choose_if_use_save_invalid_then_yes
             [PASS]    test_choose_save_file_valid
             [PASS]    test_choose_save_file_no_files
             [PASS]    test_choose_save_file_no_directory
             [PASS]    test_choose_new_game_map_valid
             [PASS]    test_choose_new_game_map_no_files
             [PASS]    test_choose_new_game_map_no_directory
             [PASS]    test_choose_player_num_valid
             [PASS]    test_choose_player_num_invalid_then_valid
             [PASS]    test_choose_player_name
             [PASS]    test_choose_player_name_invalid_space
total tests:     14
tests passed:    14
coverage: 100%
Total_unit_test_coverage:98%